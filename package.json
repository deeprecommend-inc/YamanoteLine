{
  "name": "yamanote-line-map",
  "version": "1.0.0",
  "description": "Interactive Yamanote Line station map",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && cp index.html dist/ && echo 'Build completed'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "yamanote",
    "map",
    "interactive"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
